<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">

<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
	xmlns:g='urn:import:com.google.gwt.user.client.ui'
	ui:generateFormat='com.google.gwt.i18n.rebind.format.PropertiesFormat'
	ui:generateKeys='com.google.gwt.i18n.rebind.keygen.MD5KeyGenerator'
	ui:generateLocales='default'>

		
	<ui:style type="com.fave100.client.pages.lists.widgets.listmanager.ListManagerView.ListManagerStyle">
		.container {
			margin-top: 15px;
			-webkit-touch-callout: none;
			-webkit-user-select: none;
			-khtml-user-select: none;
			-moz-user-select: -moz-none;
			-ms-user-select: none;
			user-select: none; 
		}
		
		.currentListContainer {
			border: 1px solid #bbb;
			padding: 5px;
			background-color: #f3f3f3;
			cursor: pointer;
			outline: 0;
	 	}
	 	
	 	.currentListContainer img {
			float: right;
			opacity: 0.6;
			-moz-transform: rotate(90deg);
		    -webkit-transform: rotate(90deg);
		    -o-transform: rotate(90deg);
		    -ms-transform: rotate(90deg);
		    margin-top: 2px;
	 	}
	 	
	 	.dropdownVisible img {			
			-moz-transform: rotate(-90deg);
		    -webkit-transform: rotate(-90deg);
		    -o-transform: rotate(-90deg);
		    -ms-transform: rotate(-90deg);
	 	}
	 	
		.currentList {
						
		}
		
		.listDropdown {
			position: absolute;
			width: 100%;
			background-color: #fdfdfd;
			border: 1px solid #bbb;
			border-top: 0px;		
			max-height: 250px;
			overflow-y: auto;
			/* Needs to be above facebook like button */	
			z-index: 1;
		}
		
		.addListContainer {
			padding: 5px;
			border-bottom: 1px solid #bbb;	
		}
		
		.addListContainer > * {
			width: 100%;
			display: block;
		}
		
		.listContainer {
			cursor: pointer;
			max-height: 300px;
		}
		
		.listContainer > div {			
			padding: 5px;
		}
		
		.listContainer > div:hover {
			background-color: #5eaae5;
			color: #fff;
		}
		
		.listContainer .selected {
		}
	</ui:style>
	
	
	<ui:with type="com.fave100.client.resources.img.ImageResources" field="img"/>
	
	<g:HTMLPanel ui:styleName="{style.container}">
		<g:FocusPanel ui:field="currentListContainer" ui:styleName="{style.currentListContainer}">
			<g:FlowPanel>
				<g:InlineLabel ui:field="currentList" ui:styleName="{style.currentList}" />
				<g:Image resource="{img.triangle}"/>
			</g:FlowPanel>
		</g:FocusPanel>
		<g:FlowPanel ui:field="listDropdown" ui:styleName="{style.listDropdown}">
			<g:FlowPanel ui:field="addListContainer" ui:styleName="{style.addListContainer}">
				<g:Button ui:field="addHashtagButton">Create new list</g:Button>
				<g:TextBox ui:field="listNameInput" />
			</g:FlowPanel>
			<g:FlowPanel ui:field="listContainer" ui:styleName="{style.listContainer}"/>
		</g:FlowPanel>
		<g:Label ui:field="errorMsg" ui:styleName="error" />
	</g:HTMLPanel>
</ui:UiBinder>
